<root>
	<Panel>
		<!-- V0.1 foundation HUD (state badge only). Draft panels stay hidden until later phases. -->
		<CustomUIElement type="Hud" layoutfile="file://{resources}/layout/custom_game/hud/foundation.xml" />
		<!-- Legacy draft layout kept for Phase 3+; not shown while foundation mode is active. -->
		<CustomUIElement type="Hud" layoutfile="file://{resources}/layout/custom_game/draft/draft.xml" />
	</Panel>
</root>
